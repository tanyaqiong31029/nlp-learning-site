{
  "name": "nlp-learning-site",
  "version": "1.0.0",
  "private": true,
  "description": "文科生学 NLP：零依赖纯静态学习网站（devDependencies 仅用于本地检查与 CI，运行时零依赖）",
  "type": "module",
  "scripts": {
    "lint": "eslint assets scripts && htmlhint index.html",
    "validate": "node scripts/validate-data.mjs",
    "links": "node scripts/check-links.mjs",
    "smoke": "node scripts/smoke-test.mjs http://localhost:8000",
    "update-data": "node scripts/update-data.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.30.0",
    "eslint": "^9.30.0",
    "globals": "^16.0.0",
    "htmlhint": "^1.1.4",
    "playwright": "^1.53.0"
  }
}
